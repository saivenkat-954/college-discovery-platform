{
  "name": "college-discovery-platform",
  "version": "1.0.0",
  "description": "A production-grade, full-stack college discovery platform built with **Next.js 14**, **Node.js**, **TypeScript**, and **PostgreSQL**.",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "install-all": "npm install --prefix backend && npm install --prefix frontend",
    "seed": "npm run seed --prefix backend"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
